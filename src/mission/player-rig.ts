// Measured from the active normalized AVI jetbike (height 1.8, heading 180).
// Blender receipt: art/bridgehead/hero-socket-study.json. Player root is the
// assembly base; weapon effects and collision references use this same origin.
export const PLAYER_MUZZLE_OFFSETS = [
  {x:-0.1,y:0.62,z:-2.4},
  {x:0.1,y:0.62,z:-2.4}
] as const;
