// Sound manager placeholder - sound functionality has been removed
// This empty file ensures imports don't break

export const soundManager = {
  initialize: async () => {},
  start: async () => {},
  mute: () => {},
  unmute: () => {}
};

export default function initializeSoundManager(): void {
  // This function is intentionally empty as sound functionality has been removed
}
