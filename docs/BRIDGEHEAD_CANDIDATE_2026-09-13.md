# Bridgehead engineering candidate — September 13, 2026

Red Horizon opens the full-level Bridgehead mission by default. The intended paid-game direction is unchanged. Version 2 is being verified against the saved Blender layout; it has no assigned acceptance score. The scorecard's >=90/100 gate has not been reached.

## Version 2: measured scale, whole-bike collision and visible combat

The v1 captures and input results below describe an earlier candidate. They cannot certify v2: environment scale, collision body, camera offset, route guards and combat timing changed. The current source contract is `art/bridgehead/bridgehead-v2-layout.json`, saved with `art/bridgehead/bridgehead-v2.blend` before activation and synchronized into `src/mission/bridgehead-run.ts`.

Latest visual update: the v2 Blender build now exports textured custom mesh plates with bevel normals and embedded panel/grain textures. Current route GLB SHA-256 is `9ce7bc765f3b4ae4c111f1202772a6de05e4fb58b761e2a228813908e440387b`; it is **215,660 bytes / 2,593 triangles / 5 meshes / 5 materials / 5 embedded textures**. The receipt remains `art/bridgehead/verification.json`. Blender review images were regenerated after fixing the low-route `RISE + TURN` label facing. Production build `dist/assets/index-DjLJ3PCI.js` passes 95 Node tests, typecheck and build, and the final in-app-browser Bridgehead smoke passed both routes plus loss/retry lifecycle with win resets at 75 geometries / 54 textures. Runtime captures from this build are `evidence/runtime-inspection/launch-1789330496177.png`, `fork-1789330497800.png`, `high-bridge-1789330499426.png`, `low-bridge-1789330501158.png`, `court-1789330502704.png` and `extraction-1789330504263.png`.

| Earlier defect or uncertainty | Version 2 implementation and source evidence | Evidence still required |
|---|---|---|
| The approximately 4.706 m bike dwarfed streets, and a 0.7 m sphere did not protect its nose/tail. | Source context and resident collision both use scale 3; overlay remains at identity. The base-origin bike has a conservative local envelope X±0.53, Y[-0.03,1.84], Z±2.38 m. Eighteen overlapping spheres cover that whole envelope. Translation is continuously swept; yaw uses ≤5° chord sweeps expanded to contain the intervening arc. Tests cover 100 m deltas, nose/tail, walls, narrow corridors, slopes, rotation between clear endpoints and escape from contact. | Both rendered v2 branches, actual corner/rail contacts, fallback traversal, moving silhouette and camera inspection. |
| Authored route pieces and old coordinates were not one scale contract. | The v2 receipt records **19 authored solid boxes**, **16 markers**, 217 editable Blender mesh objects and a round-tripped GLB of **215,660 bytes / 2,593 triangles / 5 meshes / 5 materials / 5 embedded textures**. Output SHA-256 is `9ce7bc765f3b4ae4c111f1202772a6de05e4fb58b761e2a228813908e440387b`; current layout hash is `48a208b2de96a44eb6ba5cf0a167f16e41b5aaccc3c88ea79ac431dd99324cbf`. Streaming detail selection converts the player back into source coordinates. | Confirm natural traversal, human readability and foreground performance on this exact production build. |
| A centered chase view let the bike obscure the aimed Warden. | The authored camera is height 2.5 m, distance 6.2 m, shoulder 1.2 m, FOV80°. Shoulder position is local to the yawed player and participates in the same world sweep. Near-plane corners are derived from the actual projection matrix, including child offset/scale and zoom, with the existing 0.25 m minimum. The shipped near=0.005 m already fit that minimum; the near-plane change is robustness, not proof of a previously observed clipping fix. | Matching launch/bridge/court moving captures and both input modes, including pause/retry. |
| Bind-pose `Box3` did not measure the displayed skinned Warden. | Actual Three r147 skinning is evaluated before normalization. `evidence/bridgehead-enemy-poses.json` records idle height 2.15 m and center approximately (0,1.075,0), with idle center excursions only 0.013/0.020/0.034 m. Punch keeps sampled torso/head inside the current direct hit volume; Swipe has much larger limb excursions. The animation API supports timed one-shots without crossfade time warping, and the death clip reaches its final pose in 0.8 s before the 0.9 s removal. | Rendered threat/lock/recovery legibility, actual aiming and hit/death feedback. The authored final corpse extends about 0.306 m below its standing base; this remains a visible-pose review detail. |
| Muzzle effects, damage rays and cover needed one spatial reference. | The saved player contract specifies twin sockets at (±0.1,0.62,-2.4) m. Weapon regression tests exercise independent cover clipping per barrel and suppression of damage feedback on rejected armored hits. The v2 contract has **425 Warden health**, a 0.95 s tracking phase, 0.25 s locked phase and 1.2 s recovery, using the tighter Punch clip. A timing fixture with 100 ms aim response records 14 high-route shots over two exposure windows versus 17 low-route shots over three; this proves the numerical reward distinction, not player mastery. | Natural visible dodge/cover/strike sequences, both rewards, paused attack/recovery and complete win/loss/retry cycles on one candidate. |

`evidence/bridgehead-collision-v2.json` records actual resident GLB geometry (**172,065 triangles**) plus the 19 authored boxes at scale 3. All **17 route legs** (seven low, ten high) reach their targets with zero recorded position or yaw error through full-body sweeps. High arrival has a clear sightline; low arrival is blocked. On Node 24.13.0 / Windows x64 the saved run has 1,000 warmed queries, **1.331 ms median / 2.747 ms p95**, with a collision build of 886.938 ms. Other builds were running concurrently. This is an offline CPU diagnostic, not a renderer frame-time, stable target-hardware comparison or performance acceptance result.

The intermediate `evidence/runtime-inspection/launch-1789329098479.png` exposed a regression: applying tight vertex bounds to the static bike enlarged it to approximately 6.062 m. That failed capture is retained. Normalization now uses posed bounds only for skinned assets; the static bike retains its established geometry `Box3`. The independent r147 audit of the actual `avi-jetbike.glb` then measures **4.705897 m length / 0.979778 m width / 1.8 m geometry-box height**, with **all 18,044 decoded vertices inside the body envelope**. Tight visible vertices span Y[0.402630,1.800000] m, a 1.397370 m height; the declared geometry-box height must not be mislabeled as the tight vertex height. The assertion and exact GLB hash are in the `staticBike` portion of the pose-audit report.

At the independent post-handback checkpoint, all **94 Node tests pass** on the 425-health contract, including layout/receipt correspondence, both forward/yaw input routes, fallback limits, directional high-gate speed and attack-window timing. Final typecheck/build/freeze are owned by the root verifier; a concurrent CapturePanel selector typing error was reported there. V2 browser captures, input sorties, asset-failure paths, resource soak and declared foreground performance remain pending in this review. Earlier placed inspection images in `evidence/runtime-inspection` are baseline diagnosis, not v2 completion or human evidence. Temporary integration failures or earlier counts must not be treated as a final result.

## Version 1 changes (historical)

- The 172,065-triangle resident coarse environment now supplies a fixed spatially indexed collision surface for flight, camera clearance and both weapons. High-detail streaming cannot replace it mid-sortie. Sphere sweeps include triangle faces, edges and vertices, with sliding on slopes.
- Blender sampling found the original Warden/extraction below terrain and the high approach clipping rock. The high route now stays at Y3.2, the lower route climbs before entering the court, the Warden stands at Y0, and extraction hovers at Y4 over a visible platform. Ordered court arrival delays the threat; the lower approach has reciprocal cover and the high arrival is exposed.
- An editable Blender route kit adds charge/shield silhouettes, broad bridge slabs, court cover, a Warden footplate and an extraction pad. The GLB is 207,480 bytes, 2,852 triangles, five materials and no textures. All seven solid proxies and eleven markers agree with the authoring receipt; original assets are preserved.
- The chase camera uses 2.5 m height / 6.2 m distance to clear the aiming center while retaining the bike silhouette. Bridgehead movement is 10 m/s, with 20 m/s boost. Dual-Shift release and pause/input clearing are repaired. The Warden remains on its court anchor and preserves its attack pose when hit.
- A missing live Warden recovers with its last observed health, on simulation time, then produces a bounded interrupted result if recovery repeatedly fails. Missing environment art retains visible collision proxies and playable mission altitude limits.
- A tactical route map and explicit climb/approach targets connect navigation to the two route rewards. Results report route, time, shots, charged shots spent and hull lost. QA remains isolated from ordinary best scores.

## Reproduce the engineering checks

```powershell
npm test
npm run typecheck
npm run build
npm run preview
node scripts/benchmark-triangle-collider.cjs evidence/bridgehead-collision.json
```

Open `http://127.0.0.1:4173/?bridgehead&playtest` and use **Run Bridgehead route smoke**, **Fly both routes with input**, or **Run 10 full input sorties**. The smoke deliberately injects route positions and short loss states. The input replay uses live key/mouse handlers and rendered simulation, without changing positions or health after launch; it still has knowledge of waypoints and enemy state and is not a human or native-pointer test.

Open `?bridgehead&playtest&asset-failure` to force the environment manifest and route GLB to fail. The same input replay should complete both fallback branches. Player/Warden hero GLBs are still loaded in this particular failure fixture.

The **Frame measurement** disclosure records three consecutive current-scene windows of 300 frames after warmup. This is a browser-scheduling diagnostic, not a GPU trace or the scorecard's three declared full-route foreground sessions. It rejects pause/focus loss and renderer resizing during collection.

## Version 1 observed evidence (historical)

- Frozen production candidate: `evidence/bridgehead-candidate-01`, SHA-256 ID `170838fed376056a0ecc27400c9d2054918e6df2c6c188e6d446dbb61a8644f9`, runtime `index-RxlphQpx.js`. The default page opens Bridgehead; `?ridge-run`, `?arena` and `?full-level` retain the earlier fixtures/survey.
- Final full-level input soak completed **ten full wins and retries** (five high/low pairs) without teleportation or direct health changes after launch. Every pair settled at **83 geometries / 51 textures / 172,065 fixed terrain triangles**. All highs used nine shots, all lows fourteen; all ten ended with 100 hull. This supplies full programmatic sortie/lifecycle evidence, not native/human or full win/loss cohort evidence. Exact run times and counts are in `evidence/bridgehead-browser-input.json`.
- Final frozen-byte smoke passed **42 assertions**, zero failures and zero captured warning/error logs. Both win resets and all nine additional short loss resets settled at **83 geometries / 51 textures**. Final combat scheduling was 119 waits, **30.6 ms median / 32.5 ms p95**, at 1280x720 renderer pixels. Native mouse capture was rejected by the embedded browser; the visible error and successful drag-aim recovery were verified.
- 71 Node tests, typecheck and build pass after the terrain/fallback integration. Tests include exported art/marker correspondence, actual flight/player handlers under asset failure, triangle face/edge/vertex sweeps, LOD-independent terrain lifetime and acceptance-validator integrity.
- Both rendered full-level branches completed through programmatic input with no position/health injection after launch: high nine shots, low fourteen, 100 hull remaining on each initial replay. Both asset-failure branches also completed, settling at 40 geometries / 4 textures with no terrain collider. These are not fresh-player results.
- The integrated scripted smoke passed ordered routes, approach guards, real GLB animation/combat, extraction/pause, results, retries and isolated QA scores. Its earlier packaged candidate settled at 96 geometries / 53 textures after wins and 97 geometries on every additional short loss reset. Later startup hiding changes may lower initial GPU counts; compare only repeated samples of the same build.
- Fixed 1280x720 embedded combat diagnostic: 117 samples, 30.3 ms median / 36.1 ms p95. The empty-page control at `tests/browser-frame-baseline.html` measured 300 samples at 31.2 ms median / 31.6 ms p95 in the same Chromium 152 environment. The embedded environment therefore does not establish the <=20 ms target; this is not proof of a hardware ceiling or a measured game speedup.
- Offline actual-GLB sweep checks clear both canonical paths at radius 0.7 m. The final saved CPU diagnostic measured 0.328 ms p95 query cost over 1,000 warmed queries, with at most 343 tested triangles per query (`evidence/bridgehead-collision.json`). The prior independent diagnostic was 0.284 ms. These exclude rendering and do not certify browser performance.
- Four final Blender composition views were inspected at 1280x720 with the runtime's 80-degree vertical FOV. Live launch camera, model, map and gate framing were inspected. Source/coarse sampled height differences have median 0.0145 m / p95 0.179 m; thin bridge edge holes motivated the exact visible broad slabs. Fine decoration/high-LOD equivalence needs further corner testing.

## Acceptance still open

The owner's commercial-rights confirmation is recorded in `BRIDGEHEAD_ITERATION_REVIEW.md`. No license records were independently inspected; do not request the same confirmation again.

No fresh players were recruited or contacted. The required five-person formative round and fresh ten-person scored cohort are unperformed. Native captured-mouse traversal, supported-browser matrix, natural full win/loss/retry cycles, fixed foreground performance and complete player-facing score halves remain open. Programmatic input wins cannot replace those observations.

Use `scripts/bridgehead-acceptance.cjs` to freeze production bytes and attach hashed evidence. It refuses to turn synthetic players, changed builds or incomplete score halves into a passing score. No publication, payment or external messaging occurred.
